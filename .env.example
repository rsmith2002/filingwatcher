# Copy to .env and fill in values for local development
# Never commit .env to git

# Your email — required by SEC for edgartools API access
EDGAR_IDENTITY=r63848@proton.me

# Local PostgreSQL connection
# If using Docker: docker run -e POSTGRES_PASSWORD=ceowatcher -e POSTGRES_USER=ceowatcher -e POSTGRES_DB=ceowatcher -p 5432:5432 postgres:16
DATABASE_URL=postgresql://ceowatcher:ceowatcher@localhost:5432/ceowatcher
